import { LoginForm } from '@/features/auth/components/loginForm.component';

export default function LoginPage() {
    return (
        <div>
            <LoginForm></LoginForm>
        </div>
    );
}
